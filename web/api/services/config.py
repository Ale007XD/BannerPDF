"""
config.py
~~~~~~~~~
Константы рендерера для сайта-конструктора баннеров.
Своя копия — не зависит от бота.
Пути к шрифтам читаются из переменной окружения FONTS_DIR.
"""

import os

# ---------------------------------------------------------------------------
# Пути
# ---------------------------------------------------------------------------
FONTS_DIR = os.getenv("FONTS_DIR", "/app/fonts")
ICC_PROFILE_PATH = os.getenv(
    "ICC_PROFILE_PATH", "/profiles/ISOcoated_v2_300_eci.icc"
)

# ---------------------------------------------------------------------------
# Параметры баннера
# ---------------------------------------------------------------------------
SAFE_ZONE_MM: int = 30       # Поля безопасности (отступ от края), мм
MIN_DIMENSION: int = 500     # Минимальный размер стороны баннера, мм
MAX_DIMENSION: int = 3000    # Максимальный размер стороны баннера, мм

# ---------------------------------------------------------------------------
# Шрифты: отображаемое имя → путь внутри контейнера
# ---------------------------------------------------------------------------
FONTS: dict[str, str] = {
    "Golos Text":     os.path.join(FONTS_DIR, "GolosText-Regular.ttf"),
    "Tenor Sans":     os.path.join(FONTS_DIR, "TenorSans-Regular.ttf"),
    "Fira Sans Cond": os.path.join(FONTS_DIR, "FiraSansCondensed-ExtraBold.ttf"),
    "PT Sans Narrow": os.path.join(FONTS_DIR, "PTSansNarrow-Bold.ttf"),
}

# ---------------------------------------------------------------------------
# Цвета: отображаемое имя → RGB (превью) + CMYK (печать, шкала 0–100)
# ---------------------------------------------------------------------------
COLORS: dict[str, dict] = {
    "Белый": {
        "cmyk":  (0,   0,   0,   0),
        "rgb":   (255, 255, 255),
        "emoji": "⚪️",
    },
    "Черный": {
        "cmyk":  (0,   0,   0,   100),
        "rgb":   (0,   0,   0),
        "emoji": "⚫️",
    },
    "Красный": {
        "cmyk":  (0,   100, 100, 0),
        "rgb":   (255, 0,   0),
        "emoji": "🔴",
    },
    "Желтый": {
        "cmyk":  (0,   0,   100, 0),
        "rgb":   (255, 255, 0),
        "emoji": "🟡",
    },
    "Синий": {
        "cmyk":  (100, 100, 0,   0),
        "rgb":   (0,   0,   255),
        "emoji": "🔵",
    },
    "Зеленый": {
        "cmyk":  (100, 0,   100, 0),
        "rgb":   (0,   128, 0),
        "emoji": "🟢",
    },
}

# ---------------------------------------------------------------------------
# Размеры баннеров: ключ → (ширина мм, высота мм)
# ---------------------------------------------------------------------------
BANNER_SIZES: dict[str, tuple[int, int]] = {
    "3x2":    (3000, 2000),
    "2x1":    (2000, 1000),
    "1x0.5":  (1000,  500),
    "1.5x1":  (1500, 1000),
    "1.5x0.5":(1500,  500),
}
