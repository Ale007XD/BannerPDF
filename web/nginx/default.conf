server {
    listen 80;
    server_name bannerbot.ru;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name bannerbot.ru;

    ssl_certificate /etc/letsencrypt/live/bannerbot.ru/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/bannerbot.ru/privkey.pem;

    # Оптимизация SSL
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    root /app/frontend;
    index index.html;

    # API Proxy
    location /api/ {
        proxy_pass http://api:8000;
        
        # Заменяем отсутствующий proxy_params прямыми директивами
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Таймауты для тяжелого рендера PDF
        proxy_read_timeout 300s;
        proxy_connect_timeout 75s;
    }

    # Static Files
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Admin Panel
    location /admin/ {
        alias /app/frontend/admin/;
        try_files $uri $uri/ /admin/index.html;
    }
}
