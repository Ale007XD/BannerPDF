# Тестовые зависимости BannerPrint
# Устанавливаются в CI поверх основных requirements.txt

pytest==8.2.0
pytest-asyncio==0.23.7
httpx==0.27.0          # уже в основных, но явно для тестов
anyio==4.3.0           # бэкенд asyncio для pytest-asyncio
